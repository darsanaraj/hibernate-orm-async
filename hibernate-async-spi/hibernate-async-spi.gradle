dependencies {
    compile( project( ':hibernate-core' ) )
    compile( "com.jakobk:db-async-java-api:1.0.0-SNAPSHOT" )
}

mavenPom {
    name = 'Hibernate Async SPI'
    description = 'Hibernate Asynchronous Database Connection SPI'
}

def osgiDescription() {
	return mavenPom.description
}
